Imports System

Module Program
    Sub Main(args As String())
        ' null - lack of value

        ' Haven't asked for the age yet
        Dim age As Integer? = Nothing
        Dim birthday As Boolean? = Nothing
        Dim battingAverage As Double? = Nothing
        Dim accountBalance As Decimal? = Nothing
        Dim firstName As String = Nothing

        ' We have asked for the age now
        age = 0

    End Sub
End Module
